{
    "name": "@front-desk-nexus-ai/server",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "dev": "uv run python -m uvicorn main:app --reload --env-file .env",
      "start": "uv run python -m uvicorn main:app --env-file .env",
    "format": "uv run --with black python -m black .",
    "format:check": "uv run --with black python -m black --check ."
    }
  }