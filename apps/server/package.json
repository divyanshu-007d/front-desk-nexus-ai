{
    "name": "@front-desk-nexus-ai/server",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "dev": "uv run uvicorn main:app --reload --env-file .env",
      "start": "uv run uvicorn main:app --env-file .env",
      "format": "uv run black .",
      "format:check": "uv run black --check ."
    }
  }